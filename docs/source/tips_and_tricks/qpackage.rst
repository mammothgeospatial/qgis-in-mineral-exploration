========
QPackage
========

The QPackage plug-in allows the user to create a project file with an associated folder holding all the relevant layers for that project. Note that this will only copy vector files.
